{
	"name": "@review-agent/cli",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"bin": {
		"review": "./src/main.ts"
	},
	"scripts": {
		"review": "bun src/main.ts",
		"build": "bun build --compile --minify --outfile=dist/review src/main.ts",
		"build:all": "bun build --compile --minify --target=bun-darwin-arm64 --outfile=dist/review-darwin-arm64 src/main.ts && bun build --compile --minify --target=bun-darwin-x64 --outfile=dist/review-darwin-x64 src/main.ts && bun build --compile --minify --target=bun-linux-arm64 --outfile=dist/review-linux-arm64 src/main.ts && bun build --compile --minify --target=bun-linux-x64 --outfile=dist/review-linux-x64 src/main.ts",
		"check-types": "tsc --noEmit",
		"test": "vitest run",
		"test:e2e": "vitest run test/e2e"
	},
	"dependencies": {
		"@review-agent/schema": "workspace:*",
		"zod": "^3.23.8"
	},
	"devDependencies": {
		"@modelcontextprotocol/sdk": "^1.29.0",
		"@types/node": "^25.6.0",
		"tsx": "^4.21.0",
		"typescript": "^5.6.3",
		"vitest": "^2.1.8"
	}
}
